# ===========================
# Filharmonia AI - Dependencies
# ===========================
#
# Only DIRECT dependencies are listed here.
# Transitive dependencies are resolved by pip automatically.
#
# PyTorch (torch, torchaudio, torchvision): NOT listed here.
# setup.sh auto-detects GPU/CPU and installs the correct build.
# Manual install:
#   CPU:      pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cpu
#   CUDA 12:  pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu121
#   CUDA 11:  pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/cu118
#   ROCm 6:   pip install torch torchaudio torchvision --index-url https://download.pytorch.org/whl/rocm6.2

# --- Web Framework ---
fastapi>=0.115.0
uvicorn>=0.30.0
python-multipart>=0.0.9
websockets>=15.0
watchfiles>=1.0.0

# --- Database ---
SQLAlchemy>=2.0.25
aiosqlite>=0.21.0

# --- Async & HTTP ---
aiohttp>=3.9.0
requests>=2.31.0
httpx>=0.26.0

# --- ML / AI ---
transformers>=4.45.0
datasets>=4.0.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# --- Audio Processing ---
librosa>=0.10.2
soundfile>=0.12.1
eyed3>=0.9.9
tqdm>=4.65.0

# --- Scientific Computing ---
numpy>=1.26.0
scipy>=1.12.0
scikit-learn>=1.4.0

# --- ONNX (CPU Optimization) ---
onnx>=1.16.0
onnxruntime>=1.18.0

# --- Data Processing ---
polars>=1.20.0
pyarrow>=15.0.0
pydantic>=2.5.0
beautifulsoup4>=4.12
pdfplumber>=0.11
pillow>=10.0.0

# --- Utilities ---
python-dotenv>=1.0.0
psutil>=5.9.0
cachetools>=5.3.0

# --- Testing ---
pytest>=8.0.0
pytest-asyncio>=0.23.0
