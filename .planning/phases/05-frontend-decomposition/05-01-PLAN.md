---
phase: 05-frontend-decomposition
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/training.py
  - backend/app/api/v1/export.py
  - frontend/package.json
  - frontend/pnpm-lock.yaml
autonomous: true

must_haves:
  truths:
    - "training.py file no longer exists in backend/app/services/"
    - "export.py sanitizes filenames with invalid characters"
    - "howler is not listed in package.json dependencies"
  artifacts:
    - path: "backend/app/api/v1/export.py"
      provides: "Filename sanitization for exported WAVs"
      contains: "re.sub.*[<>:\"|?*]"
  key_links:
    - from: "backend/app/api/v1/export.py"
      to: "generate_export_filename"
      via: "sanitize before filename creation"
      pattern: "re\\.sub.*song_name"
---

<objective>
Clean up unused dependencies and fix filename sanitization security issue.

Purpose: Remove dead code (unused Keras training service, unused howler audio library) and fix OWASP-recommended filename sanitization to prevent filesystem issues with invalid characters in exported filenames.

Output: Cleaner codebase with no unused dependencies, secure filename handling in export service.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

Files to modify:
@backend/app/services/training.py (DELETE - unused Keras training)
@backend/app/api/v1/export.py (FIX - filename sanitization)
@frontend/package.json (FIX - remove howler)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete unused training.py and sanitize export filenames</name>
  <files>
    backend/app/services/training.py
    backend/app/api/v1/export.py
  </files>
  <action>
1. DELETE `backend/app/services/training.py` entirely. This file contains the legacy Keras CNN training service (535 lines) that is NOT used anywhere - the API at `backend/app/api/v1/training.py` imports from `app.services.ast_training` instead.

2. In `backend/app/api/v1/export.py`, add filename sanitization:
   - Add `import re` at the top imports
   - In `generate_export_filename()` function (line 74-78), sanitize `song_name` before using it:
     ```python
     def generate_export_filename(song_name: str, timestamp: str, class_name: str, index: int) -> str:
         """Generate filename for exported WAV"""
         # Sanitize song_name - remove invalid filename characters per OWASP
         safe_song_name = re.sub(r'[<>:"|?*]', '_', song_name)
         # Convert timestamp HH:MM:SS to HH-MM-SS for filename safety
         safe_timestamp = timestamp.replace(':', '-')
         return f"{safe_song_name}_{safe_timestamp}_{class_name}_{index:03d}.wav"
     ```

Why: OWASP Input Validation recommends sanitizing filenames by removing/replacing characters that are invalid on various filesystems (`<>:"|?*` are invalid on Windows).
  </action>
  <verify>
    - `ls backend/app/services/training.py` returns "No such file or directory"
    - `grep -n "re.sub.*\[<>:" backend/app/api/v1/export.py` shows the sanitization line
    - Python syntax check: `python -m py_compile backend/app/api/v1/export.py` returns no errors
  </verify>
  <done>
    - training.py deleted
    - export.py contains filename sanitization using re.sub for invalid characters
    - Backend starts without import errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Remove unused howler dependency from frontend</name>
  <files>
    frontend/package.json
  </files>
  <action>
Remove howler from package.json dependencies. The StickyPlayer component uses native HTMLAudioElement, not howler.

1. Edit `frontend/package.json`:
   - Remove the line `"howler": "^2.2.4",` from dependencies

2. Run `cd /workspace/filharmonia-ai/frontend && pnpm install` to update lockfile

Why: howler is a 30KB+ library that is not used anywhere in the codebase. StickyPlayer.tsx uses native HTMLAudioElement via useRef/refs and direct DOM manipulation.

Note: @types/howler is NOT in package.json (only howler itself), so no @types removal needed.
  </action>
  <verify>
    - `grep -c howler frontend/package.json` returns 0
    - `cd frontend && pnpm ls howler 2>&1` shows howler is not installed
    - `cd frontend && pnpm build` completes successfully
  </verify>
  <done>
    - howler removed from package.json
    - pnpm-lock.yaml updated
    - Frontend builds successfully without howler
  </done>
</task>

</tasks>

<verification>
1. Backend verification:
   - `python -c "from app.services.ast_training import get_ast_training_service"` succeeds
   - `ls backend/app/services/training.py` fails (file deleted)
   - `grep "re.sub" backend/app/api/v1/export.py` shows sanitization

2. Frontend verification:
   - `cd frontend && pnpm build` succeeds
   - `grep howler frontend/package.json` returns nothing

3. Integration test:
   - Backend starts: `cd backend && timeout 5 python -c "from app.main import app; print('OK')" 2>&1 | head -5`
</verification>

<success_criteria>
1. `backend/app/services/training.py` does not exist
2. `export.py` contains `re.sub(r'[<>:"|?*]', '_', song_name)` or equivalent
3. `howler` not in `frontend/package.json`
4. Both backend and frontend build/start without errors
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-decomposition/05-01-SUMMARY.md`
</output>
