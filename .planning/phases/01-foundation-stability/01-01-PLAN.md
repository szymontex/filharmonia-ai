---
phase: 01-foundation-stability
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/main.py
  - backend/app/api/v1/analyze.py
  - backend/app/api/v1/batch.py
  - backend/app/api/v1/files.py
  - backend/app/services/ast_inference.py
  - backend/app/services/ast_training.py
autonomous: true

must_haves:
  truths:
    - "Bare except clauses replaced with specific exception types"
    - "Errors are logged with context instead of silently swallowed"
    - "Ctrl+C (KeyboardInterrupt) can stop the application"
  artifacts:
    - path: "backend/app/main.py"
      provides: "check_gpu with ImportError handling"
      contains: "except ImportError"
    - path: "backend/app/api/v1/analyze.py"
      provides: "read_job_status with json.JSONDecodeError handling"
      contains: "except json.JSONDecodeError"
    - path: "backend/app/api/v1/batch.py"
      provides: "read_job_status with json.JSONDecodeError handling"
      contains: "except json.JSONDecodeError"
    - path: "backend/app/api/v1/files.py"
      provides: "ID3 tag parsing with specific exception handling"
      contains: "except Exception as e"
    - path: "backend/app/services/ast_inference.py"
      provides: "Model loading with FileNotFoundError handling"
      contains: "except FileNotFoundError"
    - path: "backend/app/services/ast_training.py"
      provides: "Hardlink creation with OSError handling"
      contains: "except OSError"
  key_links:
    - from: "all files"
      to: "logging module"
      via: "logger.warning/error calls"
      pattern: "logger\\.(warning|error)"
---

<objective>
Replace all bare `except:` clauses with specific exception types and add logging.

Purpose: Bare `except:` catches SystemExit, KeyboardInterrupt, and GeneratorExit - making apps hard to stop and debug. This prevents silent failures and enables proper error tracking.

Output: Six backend files with proper exception handling and logging.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation-stability/01-RESEARCH.md
</context>

<references>
Requirements: CRIT-01, CRIT-02, CRIT-03, CRIT-04, CRIT-05, CRIT-06
Research patterns: "Recommended Exception Handling Pattern" from 01-RESEARCH.md
</references>

<tasks>

<task type="auto">
  <name>Task 1: Fix bare except clauses in main.py and API files</name>
  <files>
    backend/app/main.py
    backend/app/api/v1/analyze.py
    backend/app/api/v1/batch.py
    backend/app/api/v1/files.py
  </files>
  <action>
Replace bare `except:` with specific exception types and add logging:

1. **main.py:84** - `check_gpu()` function:
   - Replace `except:` with `except ImportError:`
   - This catches the case where torch is not installed
   - No logging needed (expected case)

2. **analyze.py:36** - `read_job_status()` function:
   - Replace `except:` with `except json.JSONDecodeError as e:`
   - Add logging: `logger.warning(f"Failed to parse job status {job_id}: {e}")`
   - Add `import logging` and `logger = logging.getLogger(__name__)` at top

3. **batch.py:39** - `read_job_status()` function:
   - Same pattern as analyze.py
   - Replace `except:` with `except json.JSONDecodeError as e:`
   - Add logging: `logger.warning(f"Failed to parse job status {job_id}: {e}")`

4. **batch.py:350** - In `get_outdated_csvs()` endpoint:
   - Replace `except:` with `except Exception as e:`
   - Add logging: `logger.debug(f"Could not derive MP3 path for {csv_file}: {e}")`
   - This is expected for some files so use debug level

5. **files.py:49** - ID3 tag parsing in `list_sorted_files()`:
   - Replace `except:` with `except Exception as e:`
   - Add logging: `logger.debug(f"Could not read ID3 tag from {mp3_file}: {e}")`
   - Use debug level - many files may not have ID3 tags

6. **files.py:66** - There is no line 66 except in requirements, but the actual code shows the except at line 49. Verify there are no other bare excepts in files.py.
  </action>
  <verify>
Run: `grep -n "except:" backend/app/main.py backend/app/api/v1/analyze.py backend/app/api/v1/batch.py backend/app/api/v1/files.py`
Should return no results (all bare excepts replaced).
  </verify>
  <done>
All bare `except:` clauses in main.py, analyze.py, batch.py, and files.py are replaced with specific exception types.
  </done>
</task>

<task type="auto">
  <name>Task 2: Fix bare except clauses in service files</name>
  <files>
    backend/app/services/ast_inference.py
    backend/app/services/ast_training.py
  </files>
  <action>
Replace bare `except:` with specific exception types and add logging:

1. **ast_inference.py:189-190** - In `get_ast_inference_service()`:
   - Current code: `except FileNotFoundError:` followed by bare `except:`
   - The FileNotFoundError is already handled correctly
   - Check if there are any other bare excepts in this file
   - If there are, replace with `except Exception as e:` and log appropriately

2. **ast_training.py:214** - In `_prepare_dataset_splits()` method:
   - Current code at line 214 (hardlink fallback): `except:`
   - Replace with `except OSError as e:`
   - Add logging: `logger.debug(f"Hardlink failed for {wav_file.name}, copying instead: {e}")`
   - This is expected behavior (hardlinks don't work on all filesystems)

3. **ast_training.py:812-814** - In `get_training_data_stats()`:
   - Current code: `except:` followed by fallback duration calculation
   - Replace with `except Exception as e:`
   - Add logging: `logger.debug(f"Could not read duration for {wav_file}: {e}")`

Add logging imports to both files if not present:
```python
import logging
logger = logging.getLogger(__name__)
```
  </action>
  <verify>
Run: `grep -n "except:" backend/app/services/ast_inference.py backend/app/services/ast_training.py`
Should return no results (all bare excepts replaced).
  </verify>
  <done>
All bare `except:` clauses in ast_inference.py and ast_training.py are replaced with specific exception types.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify all bare excepts are fixed</name>
  <files></files>
  <action>
Run comprehensive check across entire backend:

```bash
grep -rn "except:" backend/app/ --include="*.py" | grep -v "except Exception" | grep -v "except json" | grep -v "except File" | grep -v "except OS" | grep -v "except Import" | grep -v "except Interrupt" | grep -v "except Value" | grep -v "except Key"
```

If any bare `except:` clauses remain, fix them using the same pattern:
1. Identify the expected exception type
2. Replace with `except SpecificException as e:`
3. Add appropriate logging

For any remaining bare excepts:
- If catching truly unexpected errors, use `except Exception as e:`
- Never use bare `except:` as it catches KeyboardInterrupt
  </action>
  <verify>
Run: `grep -rn "except:" backend/app/ --include="*.py" | grep -v "# noqa" | wc -l`
Should return 0 or only commented examples.

Manual verification:
- Run the backend: `cd backend && python -m uvicorn app.main:app --reload`
- Press Ctrl+C - server should stop immediately (not hang)
  </verify>
  <done>
No bare `except:` clauses remain in the backend codebase. Ctrl+C properly terminates the application.
  </done>
</task>

</tasks>

<verification>
All Phase 1 exception handling requirements verified:

1. `grep -rn "except:" backend/app/ --include="*.py"` returns no bare except clauses
2. Each file has `import logging` and creates a logger
3. Backend starts successfully: `cd backend && python -c "from app.main import app; print('OK')"`
4. Ctrl+C terminates the running server within 2 seconds
</verification>

<success_criteria>
- CRIT-01: main.py:84 uses `except ImportError:`
- CRIT-02: analyze.py:36 uses `except json.JSONDecodeError:`
- CRIT-03: batch.py:39 uses `except json.JSONDecodeError:`
- CRIT-04: files.py:49 uses `except Exception:` with logging
- CRIT-05: ast_inference.py bare excepts replaced
- CRIT-06: ast_training.py:214 uses `except OSError:`
- All changes include appropriate logging
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-stability/01-01-SUMMARY.md`
</output>
