---
phase: 06-gpu-a-cpu-optimization
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/src/main.tsx
  - frontend/src/hooks/useAudioPlayer.ts
  - frontend/src/hooks/useExponentialPolling.ts
  - frontend/src/hooks/useAutosave.ts
  - frontend/src/hooks/useKeyboardShortcuts.ts
  - frontend/src/pages/WaveformEditor.tsx
  - frontend/src/pages/UncertaintyReview.tsx
  - frontend/src/pages/CsvViewer.tsx
  - frontend/src/components/StickyPlayer.tsx
autonomous: false

must_haves:
  truths:
    - "React 19 upgrade causes no regressions in existing functionality"
    - "All useRef calls have explicit initial values (null or appropriate default)"
    - "Application builds without TypeScript errors"
    - "All existing features work: file browser, CSV viewer, audio playback, keyboard shortcuts"
  artifacts:
    - path: "frontend/package.json"
      provides: "React 19 dependencies"
      contains: "react.*19"
  key_links:
    - from: "frontend/package.json"
      to: "frontend/src/main.tsx"
      via: "React 19 createRoot API"
      pattern: "createRoot"
---

<objective>
Upgrade React 18.3 to React 19, fixing all TypeScript breaking changes (useRef arguments, ref callbacks, JSX namespace).

Purpose: FRONT-01 requirement - React 19 upgrade with no regressions.
Output: Working application on React 19 with all features intact.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/06-gpu-a-cpu-optimization/06-RESEARCH.md
@frontend/package.json
@frontend/src/main.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade React 19 packages and fix TypeScript</name>
  <files>frontend/package.json, frontend/src/hooks/useAudioPlayer.ts, frontend/src/hooks/useExponentialPolling.ts, frontend/src/hooks/useAutosave.ts, frontend/src/hooks/useKeyboardShortcuts.ts, frontend/src/pages/WaveformEditor.tsx, frontend/src/pages/UncertaintyReview.tsx</files>
  <action>
1. Run from frontend directory:
   ```
   pnpm add react@^19.0.0 react-dom@^19.0.0
   pnpm add -D @types/react@^19.0.0 @types/react-dom@^19.0.0
   ```

2. Check if `@vitejs/plugin-react` needs update for React 19 - update if needed.

3. Fix all `useRef()` calls that lack arguments (React 19 requires explicit initial value):
   - `useRef()` with no args → `useRef(null)` or `useRef(0)` as appropriate
   - All HTMLElement refs already have `useRef<HTMLDivElement>(null)` pattern - verify these are correct
   - Mutable refs like `togglePlaybackRef`, `intervalRef`, etc. - ensure they have initial values

4. Check for ref callback implicit returns. In React 19, ref callbacks that return a value are treated as cleanup functions. Search for `ref={` patterns and ensure no arrow functions implicitly return.

5. Run `pnpm run type-check` to verify no TypeScript errors.

6. Run `pnpm run build` to verify production build succeeds.

7. Check all dependencies for React 19 compatibility:
   - @tanstack/react-query ^5.28.0 - supports React 19
   - zustand ^4.5.2 - supports React 19
   - react-router-dom ^6.22.0 - supports React 19
   - react-hook-form ^7.51.0 - supports React 19
   - recharts ^2.12.0 - check compatibility, may need update
   - lucide-react - check compatibility

If any dependency is incompatible, update it. If no compatible version exists, document and keep at current version.
  </action>
  <verify>
`pnpm run type-check` passes with no errors.
`pnpm run build` completes successfully.
`grep '"react"' frontend/package.json` shows version 19.
  </verify>
  <done>React 19 installed, all TypeScript errors fixed, build passes, dependencies compatible.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>React 19 upgrade - all components, hooks, and pages migrated</what-built>
  <how-to-verify>
1. Start dev server: `cd frontend && pnpm dev`
2. Open http://localhost:5173
3. Test these critical flows:
   - Navigate file browser - files load and display
   - Open a CSV file - track table renders with data
   - Play audio - spacebar toggles playback
   - Edit a track - number keys 1-5 change classification
   - Undo/redo - Ctrl+Z and Ctrl+Y work
   - Keyboard help - ? key shows help panel
   - Calendar browser - navigate dates, open recording
4. Check browser console for React warnings or errors
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
1. `pnpm run type-check` passes
2. `pnpm run build` succeeds
3. No React deprecation warnings in browser console
4. All existing features work without regression
</verification>

<success_criteria>
- React 19 upgrade causes no regressions in existing functionality
- TypeScript builds cleanly with React 19 types
- All hooks and components work correctly
</success_criteria>

<output>
After completion, create `.planning/phases/06-gpu-a-cpu-optimization/06-05-SUMMARY.md`
</output>
