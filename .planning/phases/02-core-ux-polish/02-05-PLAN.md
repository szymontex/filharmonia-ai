---
phase: 02-core-ux-polish
plan: 05
type: execute
wave: 2
depends_on: [02-02]
files_modified:
  - frontend/src/pages/CsvViewer.tsx
  - frontend/src/components/PlayerControls.tsx
  - frontend/src/pages/CalendarBrowser.tsx
autonomous: true

must_haves:
  truths:
    - "Progress indicator shows stage text when analysis is in flight"
    - "In-flight analysis requests are cancelled when a new one starts (debounce abort)"
    - "handlePlayRecording navigates to CsvViewer with the recording path"
  artifacts:
    - path: "frontend/src/pages/CsvViewer.tsx"
      provides: "AbortController for analysis cancellation, progress state"
    - path: "frontend/src/pages/CalendarBrowser.tsx"
      provides: "Working handlePlayRecording navigation"
  key_links:
    - from: "frontend/src/pages/CsvViewer.tsx"
      to: "axios"
      via: "AbortController signal on analysis requests"
      pattern: "AbortController|signal"
    - from: "frontend/src/pages/CalendarBrowser.tsx"
      to: "react-router"
      via: "navigate() to CsvViewer path"
      pattern: "navigate.*csv"
---

<objective>
Add progress indicators, debounce abort for analysis, and implement handlePlayRecording navigation.

Purpose: Complete the remaining UX requirements (UX-05, UX-07, CLEAN-04) -- operation feedback, cancellation, and the CalendarBrowser play button.
Output: Progress indicators on operations, AbortController cancellation, working play button in CalendarBrowser.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@frontend/src/pages/CsvViewer.tsx
@frontend/src/pages/CalendarBrowser.tsx
@frontend/src/components/PlayerControls.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add progress indicators and debounce abort to CsvViewer</name>
  <files>frontend/src/pages/CsvViewer.tsx, frontend/src/components/PlayerControls.tsx</files>
  <action>
1. **CsvViewer.tsx** -- Progress state:
   - Add state: `const [progressStage, setProgressStage] = useState<string | null>(null)`
   - When starting analysis: `setProgressStage('Analyzing...')`
   - When loading CSV: `setProgressStage('Loading...')`
   - When saving: `setProgressStage('Saving...')`
   - On completion/error: `setProgressStage(null)`
   - Pass `progressStage` to PlayerControls

2. **CsvViewer.tsx** -- AbortController for analysis cancellation (UX-05):
   - Add ref: `const abortRef = useRef<AbortController | null>(null)`
   - Before starting analysis request:
     ```typescript
     abortRef.current?.abort()  // cancel previous
     abortRef.current = new AbortController()
     ```
   - Pass `signal: abortRef.current.signal` to axios calls for analysis
   - In catch block: if `axios.isCancel(err)`, silently ignore (don't show error toast)
   - On component unmount: `abortRef.current?.abort()` in cleanup

3. **PlayerControls.tsx** -- Show progress:
   - Accept new prop: `progressStage: string | null`
   - When `progressStage` is set, show it inline (e.g., replace the analyze/save button label with the stage text, or show a small spinner + text near the buttons)
   - Use a subtle pulsing animation (TailwindCSS `animate-pulse`) on the progress text
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit`
- `grep -n "AbortController\|progressStage" frontend/src/pages/CsvViewer.tsx`
  </verify>
  <done>Progress stage shows during operations, previous analysis requests are cancelled when new one starts, cancelled requests don't trigger error toasts</done>
</task>

<task type="auto">
  <name>Task 2: Implement handlePlayRecording in CalendarBrowser</name>
  <files>frontend/src/pages/CalendarBrowser.tsx</files>
  <action>
Replace the TODO stub at `handlePlayRecording` (~line 107-110) with navigation to CsvViewer.

1. Read CalendarBrowser.tsx to understand:
   - How routing works (react-router `useNavigate`? or direct path change?)
   - What the CsvViewer route path is
   - What parameters CsvViewer expects (recording path as query param or route param?)

2. Implementation:
   - If using react-router: `navigate('/csv-viewer?path=' + encodeURIComponent(recording.path))`
   - Or whatever routing pattern the app uses -- check App.tsx / router config
   - If CsvViewer accepts a file path via URL params, use that
   - If no URL param support exists, pass via state: `navigate('/csv-viewer', { state: { path: recording.path } })`
   - Remove the `console.log` TODO line

3. Ensure `useNavigate` is imported if not already present.
  </action>
  <verify>
- `grep -n "handlePlayRecording" frontend/src/pages/CalendarBrowser.tsx` -- no more console.log/TODO
- `cd frontend && npx tsc --noEmit`
  </verify>
  <done>handlePlayRecording navigates to CsvViewer with the recording path, TODO stub removed (CLEAN-04)</done>
</task>

</tasks>

<verification>
- Progress stage visible during operations
- AbortController cancels in-flight analysis on new request
- handlePlayRecording navigates to CsvViewer (no more TODO)
- `cd frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
Progress indicators show during long operations. Analysis requests are cancellable. CalendarBrowser play button navigates to CsvViewer.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ux-polish/02-05-SUMMARY.md`
</output>
