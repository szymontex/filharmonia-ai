---
phase: 02-core-ux-polish
plan: 06
type: execute
wave: 3
depends_on: [02-01, 02-04]
files_modified:
  - frontend/src/components/KeyboardHelp.tsx
  - frontend/src/pages/CsvViewer.tsx
autonomous: true

must_haves:
  truths:
    - "Pressing ? key toggles a keyboard shortcut help panel"
    - "Help panel lists all shortcuts with their key combinations"
    - "Shortcuts are discoverable without reading documentation"
  artifacts:
    - path: "frontend/src/components/KeyboardHelp.tsx"
      provides: "Keyboard shortcut reference panel"
      exports: ["default"]
  key_links:
    - from: "frontend/src/pages/CsvViewer.tsx"
      to: "frontend/src/components/KeyboardHelp.tsx"
      via: "JSX render with toggle state"
      pattern: "<KeyboardHelp"
---

<objective>
Create keyboard shortcut help panel and wire into CsvViewer.

Purpose: Make keyboard shortcuts discoverable. Users can press ? to see all available shortcuts. Completes the UX polish for keyboard-driven workflow.
Output: KeyboardHelp component + ? key toggle in CsvViewer.
</objective>

<execution_context>
@/config/.claude/get-shit-done/workflows/execute-plan.md
@/config/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-core-ux-polish/02-01-SUMMARY.md
@.planning/phases/02-core-ux-polish/02-04-SUMMARY.md
@frontend/src/pages/CsvViewer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create KeyboardHelp panel and wire into CsvViewer</name>
  <files>frontend/src/components/KeyboardHelp.tsx, frontend/src/pages/CsvViewer.tsx</files>
  <action>
1. Create `frontend/src/components/KeyboardHelp.tsx`:
   - Props: `{ show: boolean; onClose: () => void }`
   - Renders a modal overlay (semi-transparent backdrop + centered card) with all shortcuts:
     ```
     Keyboard Shortcuts
     ------------------
     Space        Play / Pause
     Ctrl+S       Save
     Ctrl+Z       Undo
     Ctrl+Shift+Z Redo
     Ctrl+Y       Redo (alt)
     1-5          Set class (MUSIC, APPLAUSE, SPEECH, PUBLIC, TUNING)
     ?            Toggle this help
     Esc          Close this help
     ```
   - Style with TailwindCSS: fixed overlay, white card with rounded corners, shadow, grid/table layout for shortcuts
   - Close on Escape key press or clicking the backdrop
   - Two-column layout: key combination on left (monospace font, `bg-gray-100 px-2 py-1 rounded`), description on right

2. In `CsvViewer.tsx`:
   - Add state: `const [showKeyboardHelp, setShowKeyboardHelp] = useState(false)`
   - Add to the keyboard shortcuts handler map:
     - `'?'` (which is `'shift+?'` -- handle both `'?'` key): toggle `showKeyboardHelp`
     - `'escape'`: close keyboard help if open
   - Render `<KeyboardHelp show={showKeyboardHelp} onClose={() => setShowKeyboardHelp(false)} />`
   - Add a small "?" icon button in the toolbar/header area that also toggles the help panel (for mouse users)
  </action>
  <verify>
- `cd frontend && npx tsc --noEmit`
- `grep -n "KeyboardHelp" frontend/src/pages/CsvViewer.tsx` shows import and JSX
  </verify>
  <done>KeyboardHelp panel shows all shortcuts, toggles with ? key and icon button, closes with Escape</done>
</task>

</tasks>

<verification>
- KeyboardHelp.tsx exists with all shortcut listings
- CsvViewer toggles help panel with ? key
- `cd frontend && npx tsc --noEmit` passes
</verification>

<success_criteria>
Users can discover all keyboard shortcuts by pressing ? or clicking the help icon.
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-ux-polish/02-06-SUMMARY.md`
</output>
