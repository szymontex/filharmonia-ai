---
phase: 04-performance-migration
plan: 06
type: execute
wave: 3
depends_on: ["04-03", "04-04", "04-05"]
files_modified:
  - backend/requirements.txt
autonomous: true

must_haves:
  truths:
    - "pip show pandas returns 'package not found'"
    - "All CSV operations still work after pandas removal"
    - "Server starts without import errors"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "Clean requirements without pandas"
      contains: "polars"
  key_links:
    - from: "backend/requirements.txt"
      to: "polars"
      via: "dependency declaration"
      pattern: "polars"
---

<objective>
Remove pandas from requirements after migration is complete (TECH-05)

Purpose: Pandas is no longer used. Removing it reduces dependency footprint and confirms migration is complete.
Output: pandas uninstalled, requirements.txt updated, all tests pass
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-performance-migration/04-03-SUMMARY.md
@.planning/phases/04-performance-migration/04-04-SUMMARY.md
@.planning/phases/04-performance-migration/04-05-SUMMARY.md
@backend/requirements.txt
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify no pandas imports remain in codebase</name>
  <files>backend/</files>
  <action>
Before removing pandas, verify no files still import it.

Run:
```bash
grep -r "import pandas" backend/app/ --include="*.py"
grep -r "from pandas" backend/app/ --include="*.py"
```

Expected: No matches

If any matches found, those files need migration first. Do NOT proceed until all pandas imports are removed.

Also check for indirect pandas usage patterns:
```bash
grep -r "pd\." backend/app/ --include="*.py"
grep -r "\.iloc\\[" backend/app/ --include="*.py"
grep -r "\.iterrows\\(\\)" backend/app/ --include="*.py"
```

Expected: No matches (these are pandas-specific patterns)
  </action>
  <verify>
Run: `grep -r "import pandas\|from pandas\|pd\\." backend/app/ --include="*.py" | wc -l`
Expected: 0 (no pandas usage)
  </verify>
  <done>Verified no pandas imports or usage patterns remain</done>
</task>

<task type="auto">
  <name>Task 2: Remove pandas from requirements.txt</name>
  <files>backend/requirements.txt</files>
  <action>
Remove the pandas line from requirements.txt.

Current line (approximately line 68):
```
pandas==2.2.0
```

Delete this line entirely.

Also verify polars is present (should have been added in 04-01):
```
polars==1.23.0
```
  </action>
  <verify>
Run: `grep "^pandas" backend/requirements.txt`
Expected: No matches
Run: `grep "^polars" backend/requirements.txt`
Expected: Shows polars line
  </verify>
  <done>pandas removed from requirements.txt, polars remains</done>
</task>

<task type="auto">
  <name>Task 3: Uninstall pandas and verify server starts</name>
  <files>None (environment operation)</files>
  <action>
Uninstall pandas from the environment:
```bash
pip uninstall pandas -y
```

Verify it's gone:
```bash
pip show pandas
```
Expected: "WARNING: Package(s) not found: pandas"

Then verify the server can start without pandas:
```bash
cd /workspace/filharmonia-ai/backend
python -c "from app.main import app; print('Server imports OK')"
```

If any import errors occur, they will indicate which files still depend on pandas.

Note: Some dependencies might have pandas as an optional dependency. That's fine - we're removing our direct usage, not blocking transitive dependencies.
  </action>
  <verify>
Run: `pip show pandas 2>&1 | head -1`
Expected: Contains "WARNING" or "not found"
Run: `cd /workspace/filharmonia-ai/backend && python -c "from app.main import app; print('OK')"`
Expected: Prints "OK" without errors
  </verify>
  <done>pandas uninstalled, server starts successfully without it</done>
</task>

</tasks>

<verification>
1. `pip show pandas` - returns "package not found"
2. `grep "pandas" backend/requirements.txt` - no matches
3. `python -c "from app.main import app"` - no import errors
4. Start server: `uvicorn app.main:app` - starts without errors
5. Test CSV endpoint: `curl "http://localhost:8000/api/v1/csv/parse?path=..."` - works
6. Test uncertainty endpoint: `curl "http://localhost:8000/api/v1/uncertainty/stats"` - works
7. Test batch endpoint: `curl "http://localhost:8000/api/v1/analyze/outdated-csvs"` - works
</verification>

<success_criteria>
1. `pip show pandas` returns "package not found"
2. pandas line removed from requirements.txt
3. polars line present in requirements.txt
4. No grep matches for pandas imports in backend/app/
5. Server starts without import errors
6. All CSV, uncertainty, and batch endpoints functional
</success_criteria>

<output>
After completion, create `.planning/phases/04-performance-migration/04-06-SUMMARY.md`
</output>
