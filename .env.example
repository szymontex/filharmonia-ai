# Filharmonia AI - Environment Variables Configuration
# Copy this file to .env and fill in your values

# =============================================================================
# DATA DIRECTORY CONFIGURATION
# =============================================================================

# Base directory for all Filharmonia data
# If not set, defaults to: project_root/FILHARMONIA_DATA
#
# Examples:
#   Windows (local):    FILHARMONIA_BASE_DIR=C:\Users\YourName\FilharmoniaData
#   Windows (network):  FILHARMONIA_BASE_DIR=Y:\!_FILHARMONIA
#   Linux/Mac:          FILHARMONIA_BASE_DIR=/home/user/filharmonia_data
#
FILHARMONIA_BASE_DIR=

# =============================================================================
# FOLDER NAMES (optional - customize if your folders have different names)
# =============================================================================

# Defaults shown below - uncomment and change if your folders are named differently
# SORTED_FOLDER_NAME=SORTED
# NAGRANIA_FOLDER_NAME=NAGRANIA_KONCERTOW
# TRAINING_DATA_FOLDER_NAME=TRAINING_DATA
# MODELS_FOLDER_NAME=RECOGNITION_MODELS
# ML_EXPERIMENTS_FOLDER_NAME=ML_EXPERIMENTS
#
# Expected folder structure inside FILHARMONIA_BASE_DIR:
#   FILHARMONIA_BASE_DIR/
#   ├── SORTED/                    # Sorted concert recordings
#   ├── NAGRANIA_KONCERTOW/        # Raw unsorted recordings
#   ├── TRAINING_DATA/DATA/        # Training samples (5 class subfolders)
#   ├── RECOGNITION_MODELS/        # Trained PyTorch models (.pth files)
#   └── ML_EXPERIMENTS/datasets/   # Pre-split datasets for training
#

# =============================================================================
# DATABASE (optional - uses default if not set)
# =============================================================================

# SQLite database location
# Defaults to: FILHARMONIA_BASE_DIR/.claude/filharmonia.db
# DATABASE_URL=sqlite:///path/to/custom/database.db

# =============================================================================
# API SETTINGS (optional)
# =============================================================================

# CORS origins (comma-separated)
# Defaults to: http://localhost:5173,http://localhost:3000
# CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://192.168.1.100:5173

# =============================================================================
# NOTES FOR FIRST-TIME SETUP
# =============================================================================

# 1. Copy this file:
#    cp .env.example .env
#
# 2. Set FILHARMONIA_BASE_DIR to your data location
#
# 3. Create the required folder structure (see above)
#
# 4. Prepare training data:
#    - Place WAV files in TRAINING_DATA/DATA/ with 5 class subfolders:
#      - APPLAUSE/
#      - MUSIC/
#      - PUBLIC/
#      - SPEECH/
#      - TUNING/
#
# 5. (Optional) Download a pre-trained model or train your own via web UI
#
# 6. Start the backend:
#    cd backend
#    python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
